diff --git i/modules/services/Visibilities.qml w/modules/services/Visibilities.qml
index ee97b092..c7d71605 100644
--- i/modules/services/Visibilities.qml
+++ w/modules/services/Visibilities.qml
@@ -207,2 +207,3 @@ Singleton {
             property bool launcher: false
+            property bool packages: false
             property bool dashboard: false
@@ -215,2 +216,7 @@ Singleton {
 
+    // and00pium.pkg-launcher: registers the packages module without editing
+    // the moduleNames literal — the array is mutated in place, so this composes
+    // with any other mod that appends to the same literal.
+    Component.onCompleted: moduleNames.push("packages")
+
     function clearAll() {
